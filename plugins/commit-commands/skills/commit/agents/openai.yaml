interface:
  display_name: "Commit Changes"
  short_description: "Commit with current Codex model and effort attribution"
  default_prompt: "Use $commit to create one commit from my current changes."

policy:
  allow_implicit_invocation: true
