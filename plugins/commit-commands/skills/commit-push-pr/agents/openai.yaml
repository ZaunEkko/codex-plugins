interface:
  display_name: "Commit, Push, and Open PR"
  short_description: "Safely publish a work branch and open a PR"
  default_prompt: "Use $commit-push-pr to commit changes, publish the branch, and open a pull request."

policy:
  allow_implicit_invocation: true
