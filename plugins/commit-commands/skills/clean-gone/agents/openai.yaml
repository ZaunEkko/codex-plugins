interface:
  display_name: "Clean Gone Branches"
  short_description: "Force-remove local Git branches marked gone"
  default_prompt: "Use $clean-gone to force-clean local branches whose upstream is gone."

policy:
  allow_implicit_invocation: true
